{
  // just pseudo code that creates two variables

  hash pseudo = [
    // another comment
    "This is just" => "really nice pseudo code",
    // and another useless comment
    "and really nothing" => "fancy"
  ];

  hash code = [
    // this comment is superfluous
    "Anyhow we are creating" => "another hash variable",
    // as well as this one
    "which also has two" => "hash entries, like the one above"
  ];

}
